/**
 * Master Toggle for Temporary Content Archiving Mode.
 * Set `TEMPORARY_ARCHIVE_MODE = true` to hide President Cards/Pages, the Hall of Excellence, and manually uploaded/gallery photos.
 * Set `TEMPORARY_ARCHIVE_MODE = false` to restore all content instantly to its original state.
 */
export const TEMPORARY_ARCHIVE_MODE = true;
